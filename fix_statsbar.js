const fs = require('fs'); let code = fs.readFileSync('components/sections/StatsBar.jsx', 'utf8'); code = code.replace('import getPropertyCount from \\'@/app/actions/getPropertyCount\\';', 'import getPropertyCount from \\'@/app/actions/getPropertyCount\\';\\nimport getReviewStats from \\'@/app/actions/getReviewStats\\';'); code = code.replace('const [propertyCount, setPropertyCount] = useState(500);', 'const [propertyCount, setPropertyCount] = useState(500);\\n  const [reviewStats, setReviewStats] = useState({ count: 19, average: 4.8 });'); code = code.replace('getPropertyCount().then(setPropertyCount);', 'getPropertyCount().then(setPropertyCount);\\n    getReviewStats().then(setReviewStats);'); code = code.replace('{ value: 4.8, suffix: \\'\\', label: \\'Reseñas\\', decimals: 1 },', '{ value: reviewStats.average, suffix: \\'\\', label: \\'Reseñas\\', decimals: 1 },'); code = code.replace('{ value: 4.8, suffix: \\'\\', label: \\'ReseAas\\', decimals: 1 },', '{ value: reviewStats.average, suffix: \\'\\', label: \\'ReseAas\\', decimals: 1 },'); fs.writeFileSync('components/sections/StatsBar.jsx', code);
