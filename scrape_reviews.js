const puppeteer = require('puppeteer'); const mongoose = require('mongoose'); require('dotenv').config(); async function run() { await mongoose.connect(process.env.MONGODB_URI); const db = mongoose.connection.db; const browser = await puppeteer.launch({ headless: 'new' }); const page = await browser.newPage(); await page.goto('https://www.google.com/search?q=Inmobiliaria+Farias+%26+Asociados+San+Francisco+Cordoba', { waitUntil: 'networkidle2' }); await page.waitForSelector('.gws-localreviews__google-review'); const reviews = await page.evaluate(() => { const reviewElements = document.querySelectorAll('.gws-localreviews__google-review'); return Array.from(reviewElements).map(el => { const authorName = el.querySelector('.TSUbDb a')?.innerText; const text = el.querySelector('.Jtu6Td span')?.innerText; const ratingStr = el.querySelector('.fzvWSb')?.getAttribute('aria-label'); const rating = ratingStr ? parseFloat(ratingStr) : 5; const publishTimeStr = el.querySelector('.dehysf')?.innerText; return { authorName, text, rating, publishTimeStr, publishTime: new Date() }; }); }); console.log(reviews); await browser.close(); process.exit(0); } run().catch(console.error);
