require('dotenv').config(); const mongoose = require('mongoose'); async function run() { await mongoose.connect(process.env.MONGODB_URI); const db = mongoose.connection.db; await db.collection('siteconfigs').updateOne({}, { $set: { aboutText: 'En Farías y Asociados somos especialistas en el mercado inmobiliario urbano y rural. Ofrecemos un servicio integral que incluye administración de alquileres, venta de propiedades y campos, y tasaciones residenciales e industriales. Contamos con un equipo de tasadores profesionales listo para asesorarte en cada etapa, ya sea que quieras vender, alquilar o valuar tu propiedad.' } }); console.log('Updated in DB'); process.exit(0); } run();
